# Coding Plan Monitor 配置文件
# 复制此文件为 config.yaml 并填入你的 Token

glm:
  enabled: true
  accounts:
    - name: "主账号"
      token: "${GLM_TOKEN_1}"
    # - name: "龙虾号"
    #   token: "${GLM_TOKEN_2}"

# coze:
#   enabled: false
#   accounts:
#     - name: "扣子账号"
#       token: "${COZE_TOKEN}"

# kimi:
#   enabled: false
#   token: "${KIMI_TOKEN}"
